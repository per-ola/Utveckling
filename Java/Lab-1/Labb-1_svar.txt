DB2004

Labb 1

1.


public class Vinkel{

    public static void main(String[] args){

	double vinkel=Double.parseDouble(args[0]);

	double vinkel_rad=Math.toRadians(vinkel);

	System.out.print(Math.sin(vinkel_rad) * Math.sin(vinkel_rad) + Math.cos(vinkel_rad) * Math.cos(vinkel_rad));
    }
}

Jag använder typen double.

2.

public class LabbEtt{

	public static void main(String[] args){

    	int a=10;
	int b=3;

	System.out.print((double)a/b);
	}
}

3.

a)

"2bc"

b)

"5bc"

c)

"5bc"

d)

"bc5"

e)

"bc23"

4.

public class Rest{

    public static void main(String [] cmdLn){

	int a = Integer.parseInt(cmdLn[0]);

	int b = Integer.parseInt(cmdLn[1]);

	int c = a%b;

	if (c==0){

	    System.out.print("true");
	}

    }

}

5.

public class Dice_1{

    public static void main(String[] args){

	System.out.println(((int) (Math.random()*6+1)));
    }
}

6.

public class Dice_2{

    public static void main(String[] args){

	int dice_1=((int) (Math.random() * 6 + 1));
	int dice_2=((int) (Math.random() * 6 + 1));

	System.out.println(dice_1 + " " + dice_2) ;
    }
}

7.

Pseudocode:

1. Skapa fil med samma namn som programmets huvudklassnamn.

2. Deklarera klassnamn för källkoden.

3. Deklarera en main-metod som är:

  a. Publik d,v.s. är tillgänglig från varje annan plats i koden.
  
  b. Statisk, d.v.s. inte går att modifiera från annan plats i koden.
  
  c. Void, d.v.s. inte förväntar sig någon indata.
  
  d. Tar strängargument från kommandoraden.
  
4. Skapa tre variabler; d, m, y, för dag, månad och år resp. som tar tre strängar från kommandoraden representerande dag, månad och år och 	   typkastar dessa till Integer.

5. Använd färdig ekvation för beräkning av veckodag i den Gregorianska kalendern utifrån tidigare i koden genererade variabler.

6. Skriv ut resultatet i form av 0-6 för söndag-lör.

public class VeckoDag {
    public static void main(String[] args) { 
        int d = Integer.parseInt(args[0]);
        int m = Integer.parseInt(args[1]);
        int y = Integer.parseInt(args[2]);

        int y0 = y - (14 - m) / 12;
        int x = y0 + y0/4 - y0/100 + y0/400;
        int m0 = m + 12 * ((14 - m) / 12) - 2;
        int d0 = (d + x + (31*m0)/12) % 7;

        System.out.println(d0);
    }

}


